function recoverSigner metoduna gpt nin önerisiyle

bytes32 prefixedHash = keccak256(abi.encodePacked("\x19Ethereum Signed Message:\n32", hash)); 

ekledim ama aslında mint ve update fonksiyonları içinde bu iş yapılıyordu, gözden kaçırmışım
ayrıca gpt sunucu tarafında 

php ler içindeki function createSignature veya
imzaDogrulama klasörü içindeki imzalama js lerine

"\x19Ethereum Signed Message:\n32" 

standardını uygulamamı istedi, bunları yaptığım zaman kontratı çalıştıramadım, tenderly ile bir tx hash i
aynı imza ile farklı adres, token ıd veya farklı price bilgisi ile yeniden çalıştırmayı denediğimde imza geçersiz hatası aldım
yani aynı imzanın farklı bir işlemde kullanılmasının mümkün olmadığını gördüm bu yüzden v31 kullanılmayacak